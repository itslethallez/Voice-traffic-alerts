/** Shared between RadarMap.tsx's static alert pins and PoliceLightsPin.tsx's
 * animated one, so the two stay visually identical in size. */
export const ALERT_PIN_SIZE = 34;
export const ALERT_PIN_BORDER_WIDTH = 2;
